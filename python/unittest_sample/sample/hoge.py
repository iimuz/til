def check_equal(src1: int, src2: int) -> bool:
    """入力された 2 つの引数が一致する場合に true を返す

    Args:
        src1 (int): 一致チェックする第一引数
        src2 (int): 一致チェックする第二引数

    Returns:
        bool: true の場合に一致する
    """
    return src1 == src2
